name: 04 - Using Actions

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@4
            - name: Printing folders
              run: ls

        #0. Checkout the code from the repository
        #1: Install dep of our react applications
        #2. Execute automated tests